package org.lufi.giftstop.repo;

import org.lufi.giftstop.model.ProductVariantField;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProductVariantFieldRepository extends JpaRepository<ProductVariantField, Long> {
}
